# 小程序 AI 工作说明

- 本文件只约束当前 `jiangnanxi-miniprogram/` 小程序及其子目录，不影响后端、管理后台、AI Demo 或其他项目。
- Git commit 信息使用中文，说明实际改动。
- 除非用户明确要求，否则不要创建 commit。
- 使用 pnpm 安装、添加、更新和移除依赖；Windows 下使用 `pnpm.cmd`。
- 修改前检查 `git status --short`，保留其他会话的已有改动。
- 项目总导航在上级目录的 `AGENTS.md` 和 `项目导航.md`；仅维护项目内说明，不修改全局配置或记忆。
- 修改后按范围运行 `pnpm.cmd run typecheck` 和微信开发者工具验证。

## 界面设计基准

- 整体定位是现代东方珠宝风格：干净、克制、以手串和商品图片为视觉主体。宫小珠只作为布局、字号和层级参考，不复刻品牌内容，也不新增本项目没有的功能。
- 主背景使用暖浅灰 `#f5f4f3`，内容面使用白色；卡片采用轻圆角和弱阴影。
- 标题与正文使用暖深灰 `#272321/#332e2b`，辅助文字使用 `#6f6864/#9d9692`，避免把按钮、图标和状态全部做成黑色。
- 普通交互使用杏橙 `#e18a4b`，服务图标使用亮杏橙 `#f2a05f`；珊瑚红 `#ff3152` 小面积用于底部导航选中、分类下划线、订单数字和支付按钮。
- 明黄只用于优惠、活动等小面积元素。商品价格使用深灰或暖棕，不使用鲜红。
- 正文优先使用 `PingFang SC` 系统黑体；价格、数量和作品数使用 `DIN Alternate`；展示宋体只用于启动页和少量宣传语。普通业务页面不添加英文眉题，也不要使用过大的中文字距。
- 常用正文和按钮使用 `25–29rpx`，卡片标题使用 `28–32rpx`，页面与分区标题使用 `34–38rpx`；`18–22rpx` 只用于徽标、时间等次要信息，避免业务正文再次缩小。
- 卡片圆角通常控制在 `20–28rpx`。主按钮使用实底，次按钮使用浅灰或描边；选中状态优先通过字重、下划线或小面积颜色表达。
- 空状态和加载状态使用暖灰配少量杏橙，错误状态使用低饱和珊瑚红。
- `pages/diy/` 与 DIY 专用组件当前等待重构，其他页面的视觉优化不要修改这些目录。
- 用户要求“只改样式”时，保留现有页面结构、数据、跳转和业务逻辑，不补功能。涉及交互状态时，主动验证未选中、已选中、加载、空数据和失败等相关状态，不等用户提醒。
